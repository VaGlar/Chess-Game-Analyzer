[pytest]
# Without this, bare `pytest` (unlike `python -m pytest`) doesn't put the
# repo root on sys.path, so `import chess_analyzer` fails — worked locally
# only because `python -m pytest` was used, broke in CI which runs `pytest`.
pythonpath = .
